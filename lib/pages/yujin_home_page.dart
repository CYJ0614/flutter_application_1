import 'package:flutter/material.dart';

class YujinHomePage extends StatelessWidget {
  const YujinHomePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Yujin Home Page'),
      ),
      body: const Center(
        child: Text('center'),
      ),
    );
  }
}
